// ─── SiteTour — Spotlight overlay tour for new users ─────────────────────────
//
// Phase 0 placeholder: tour state (tourActive, tourStep, advanceTour,
// dismissTour) was dropped from the store in Task 0.4 and the tour targets
// referenced pages that no longer exist. Render nothing for now; a proper
// onboarding flow will be designed once the coach has shipped.
export function SiteTour() {
  return null
}
